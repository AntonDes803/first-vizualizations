import styles from "./ServicesSection.module.scss";

const ServicesSection = ({ lang, dictionary }) => {
  return (
    <section className="section">
      <div className="container">ServicesSection</div>
    </section>
  );
};

export default ServicesSection;
